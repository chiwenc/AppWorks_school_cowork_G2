endpoints include a list of products filtered by category
---
parameters:
  - name: category
    in: path
    type: string
    enum: ["all", "men", "women", "accessories"]
    required: true
    default: all
# definitions:
#   Palette:
#     type: object
#     properties:
#       palette_name:
#         type: array
#         items:
#           $ref: '#/definitions/Color'
#   Color:
#     type: string
responses:
  200:
    description: A list of products (may be filtered by category) [6 items/page]
    examples:
      {"rgb": ["red", "green", "blue"]}